---
description: Build the PreachPoint project
---

# Build Project

This workflow installs dependencies, typechecks the Next.js project, and builds the production bundle.

1. Install dependencies
// turbo
```bash
npm install
```

2. Typecheck and build
// turbo
```bash
npm run typecheck && npm run build
```
