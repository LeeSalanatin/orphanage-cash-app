
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Grant global read/write access to all authenticated users as requested.
    // This allows both admins and regular users to access all sessions, groups, and events.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
