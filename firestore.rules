
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Universal access for all authenticated users to all documents and collection groups.
    // This supports global visibility, admin management, and cross-session historical aggregation.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
